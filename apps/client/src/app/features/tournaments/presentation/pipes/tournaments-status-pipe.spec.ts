import { TournamentsStatusPipe } from './tournaments-status-pipe';

describe('TournamentsStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TournamentsStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
